# B-6 — binrpc 4.0 rewrite without abandoned deps

**✅ done 2026-09-02** — delivered upstream in the binrpc repo rather than
here: binrpc 4.1.0 replaced the unmaintained `binary`/`put` packages with
plain Buffer reads/writes (wire format verified byte-identical against
4.0.0 with 20000 fuzzed frames), 4.2.0 added fragmented-TCP-frame
reassembly and truncated-payload guards. node-red-contrib-ccu picked up
`binrpc ^4.2.0` in `4.1.0-dev.4`, with an override so the hm-simulator
test dependency uses it too — `binary`/`put` are gone from the entire
dependency tree, and the test suite exercises the 4.2.0 server.

Original item: | B-6 | binrpc 4.0 rewrite without abandoned `binary`/`put` deps | deferred, mirrors hm2mqtt §6.4 |
